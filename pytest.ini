[pytest]
markers =
    api: Тесты API
    positive: Позитивные тесты
    negative: Негативные тесты

# Настройки логирования
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s - %(levelname)s - %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Дополнительные настройки для подробных логов
addopts =
    -v
    --tb=short
    --strict-markers